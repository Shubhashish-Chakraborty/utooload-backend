services:
  backend:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./secrets:/app/secrets
    environment:
      - PYTHONUNBUFFERED=1
    command: uvicorn api.index:app --host 0.0.0.0 --port 8000